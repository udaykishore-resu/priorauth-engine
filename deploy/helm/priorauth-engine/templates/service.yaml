apiVersion: v1
kind: Service
metadata:
  name: {{ include "priorauth-engine.fullname" . }}
  labels:
    {{- include "priorauth-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "priorauth-engine.selectorLabels" . | nindent 4 }}
